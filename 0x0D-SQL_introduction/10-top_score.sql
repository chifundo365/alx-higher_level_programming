-- List all records of the table 'second_table' of database hbtn_0c_0 in MySql Server
-- Records are ordered by the score column (descending order)
SELECT score, name FROM second_table ORDER BY score DESC;
